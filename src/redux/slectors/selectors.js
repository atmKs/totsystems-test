export const foldersSelector = (state) => state.folderItems.folders;
export const emailsSelector = (state) => state.emailItems.emails;
export const modalSelector = (state) => state.folderItems.modalActive;
export const currentFolderSelector = (state) => state.folderItems.currentFolder;
export const searchTextSelector = (state) => state.emailItems.searchText;
